; Package list for FDI install media

; These are the minimum packages required to run the FreeDOS Installer (FDI).
; They are present on the filesystem for all of the install media.

base\command
base\kernel
base\deltree
base\xcopy
base\devload
base\attrib
base\shsucdx
util\shsufdrv
base\fdisk
base\fdapm
base\format
base\fc

util\v8power
util\fdimples

unixlike\grep

base\lbacache

util\fdnpkg
archiver\zip

; Only required for CD install media

util\udvd2
util\gcdrom

; Only wanted / needed for QEMU RAM Disk
util\srdisk

; These are not technically required. But, can be useful.

base\himemx
base\jemm
base\edit
base\mem
base\more
util\doslfn
