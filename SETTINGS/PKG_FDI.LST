; Package list for FDI install media

; These are the minimum packages required to run the FreeDOS Installer (FDI).
; They are present on the filesystem for all of the install media.

base\freecom
base\kernel
base\deltree
base\xcopy
base\devload
base\attrib
base\shsucdx
drivers\shsufdrv
base\fdisk
base\fdapm
base\format
base\fc

util\v8power
apps\fdimples

unix\grep

; drivers\uhdd
drivers\udvd2
drivers\cdrcache
base\lbacache

util\fdnpkg
archiver\zip

base\fdhelper

; Only required for CD install media

drivers\udvd2
; drivers\gcdrom

; El Torito driver is included through config options for CD boot media
; drivers\eltorito

; Only wanted / needed for QEMU RAM Disk
drivers\srdisk

; These are not technically required. But, can be useful.

base\himemx
base\jemm
base\edit
base\mem
base\more
drivers\doslfn
