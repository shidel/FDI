@echo off

REM Reserved for custom error message displaying.

REM If a language file FDERROR.DEF exists, it will be automatically loaded
REM prior to running this file.

REM If there is an error or the script is aborted, this file will be called
REM after the main cleanup stage and the built-in messages are shown. But,
REM is called before the script will reboot or terminate.

